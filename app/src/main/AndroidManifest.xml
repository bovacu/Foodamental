<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bovazque.registro_activity">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RecetasPorIngredientes"></activity>
        <activity android:name=".TipoBusquedaReceta" />
        <activity android:name=".RecetasEncontradas_activity" />
        <activity android:name=".RecetasPorCategoria" />
        <activity android:name=".Principal_activity" >

        </activity>
        <activity android:name=".Inicio_activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".InicioSesion_activity" />
        <activity android:name=".Registro_activity" />
        <activity android:name=".VerReceta_activity" />
        <activity android:name=".Lista_activity"></activity>
    </application>

</manifest>